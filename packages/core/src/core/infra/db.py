"""Async Postgres adapter — engine + session (spec §5).

STUB. Rung 0 / Task 0.3 implements:
  get_engine() -> AsyncEngine
  session() -> AsyncSession     (async context manager)
Built on SQLAlchemy 2.0 async + asyncpg, reading settings.DATABASE_URL.
"""
