"""MinIO adapter — object bytes (spec §5, §7).

STUB. Rung 1 / Task 1.3 implements:
  ensure_bucket(), put_text(key, str), put_bytes(), get_text(), get_bytes(),
  list_prefix(prefix)
Bucket `audio-drama`: raw/<job>.txt, tts/<hash>.wav, out/<job>.mp3.
"""
